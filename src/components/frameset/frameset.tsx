import React from "react";

export interface FramesetProps {}

const Frameset: React.FC<FramesetProps> = (props) => {
  //
  return <frameset cols="50%, 50%"></frameset>;
};

export default Frameset;
