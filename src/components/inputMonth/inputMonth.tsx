import React from "react";

export interface InputMonthProps { 
}

const InputMonth : React.FC<InputMonthProps> = (props) => {
  
  //
  return (
    <input
      type="month" 
    />
  );
};

export default InputMonth;
